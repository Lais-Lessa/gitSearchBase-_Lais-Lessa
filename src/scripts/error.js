async function newSearch(){
    const button = document.getElementById("button__error");

    button.addEventListener('click', async(e)=>{
        e.preventDefault
        window.history.back();

    })
}
newSearch()